server:
  port: 8189
  servlet:
    context-path: /task-manager
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=task-manager-dir
    username: postgres
    password: 374563aA
  timeleaf:
    encoding: UTF-8
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect
        temp:
          use_jdbc_metadata_defaults: false